% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sra_import.R
\name{BascetResolveSra}
\alias{BascetResolveSra}
\title{Resolve SRA inputs to a canonical run table}
\usage{
BascetResolveSra(
  runs = NULL,
  sralist = NULL,
  runinfo = NULL,
  query = NULL,
  bioproject = NULL,
  terms = NULL,
  pageSize = 500,
  sleep = 1.2
)
}
\arguments{
\item{runs}{Character vector of SRA run accessions.}

\item{sralist}{Path to a file with one SRA run accession per line.}

\item{runinfo}{SRA RunInfo data frame, or path to a RunInfo CSV file.}

\item{query}{NCBI SRA query passed to \code{rentrez}.}

\item{bioproject}{BioProject accession. Used to build a query when \code{query}
is not provided.}

\item{terms}{Additional NCBI SRA query terms joined with \code{AND}.}

\item{pageSize}{Number of records fetched per E-utilities page.}

\item{sleep}{Seconds to sleep between paged E-utilities fetches.}
}
\value{
A canonical SRA run table.
}
\description{
Accepts direct SRR accessions, an \code{.sralist}, an existing RunInfo CSV/data
frame, or an NCBI SRA query. The returned table always contains \code{Run} and
\code{cell}, with \code{cell} defaulting to \code{Run} to avoid collisions. Other RunInfo
columns are retained as a mapping table for later renaming.
}
