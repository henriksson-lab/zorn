% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ncbi_genomes.R
\name{BascetDownloadNcbiGenomeMetadata}
\alias{BascetDownloadNcbiGenomeMetadata}
\title{Download and cache an NCBI genome assembly summary}
\usage{
BascetDownloadNcbiGenomeMetadata(
  db = "refseq",
  group = "bacteria",
  cacheDir = defaultNcbiGenomeCacheDir(),
  overwrite = FALSE,
  dest = NULL
)
}
\arguments{
\item{db}{NCBI source database, usually \code{"refseq"} or \code{"genbank"}.}

\item{group}{NCBI genome group, default \code{"bacteria"}.}

\item{cacheDir}{Cache root directory. Defaults to the Zorn cache under the
user cache directory.}

\item{overwrite}{Re-download when the cached file already exists.}

\item{dest}{Optional explicit output file path. When provided, this path is
used instead of \code{file.path(cacheDir, db, group, "assembly_summary.txt")}.}
}
\value{
Path to the cached \code{assembly_summary.txt}.
}
\description{
Downloads the NCBI \code{assembly_summary.txt} metadata file for a RefSeq/GenBank
group. By default this caches RefSeq bacteria metadata under
\code{file.path(tools::R_user_dir("Zorn", "cache"), "ncbi", "refseq", "bacteria", "assembly_summary.txt")}. This is platform-specific and portable
across Linux, macOS, and Windows. If the cached file already exists, it is
reused unless \code{overwrite = TRUE}.
}
