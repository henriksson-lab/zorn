# Create new no-runner instance, used for debugging

Create new no-runner instance, used for debugging

## Usage

``` r
NoRunner(showScript = TRUE)
```

## Arguments

- showScript:

  For debugging: print script to run

## Value

A NoRunner instance
