# Create new BascetCountMatrix

Create new BascetCountMatrix

## Usage

``` r
NewBascetCountMatrix(X, obs)
```

## Arguments

- X:

  Sparse count matrix (dgCMatrix or similar)

- obs:

  Data frame of observation metadata, one row per cell

## Value

A Bascet count matrix object
