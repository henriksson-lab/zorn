# Check that parameter is in the form "xxx%"

Check that parameter is in the form "xxx%"

## Usage

``` r
is.percent.string(s)
```

## Arguments

- s:

  A string to test for percent format (e.g. "10%")
