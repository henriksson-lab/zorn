# Local job

Job object for a locally launched process.

## Slots

- `cmd`:

  Command string.

- `proc`:

  Process handle.

- `logfile`:

  Path to the job log file.

- `arraysize`:

  Number of array tasks.
