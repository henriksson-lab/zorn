# Local runner

Runner backend that executes Bascet jobs locally.

## Slots

- `ncpu`:

  Number of CPU cores to use.

- `mem`:

  Memory limit string.

- `direct`:

  Whether jobs are run synchronously.

- `showScript`:

  Whether to show generated scripts for debugging.
