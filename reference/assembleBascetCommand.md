# Prepare Bascet command given arguments

Prepare Bascet command given arguments

## Usage

``` r
assembleBascetCommand(bascetInstance, params)
```

## Arguments

- bascetInstance:

  bascetInstance

- params:

  List of parameters

## Value

Bascet command to run as a string
