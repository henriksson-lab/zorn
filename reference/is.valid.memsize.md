# Check that parameter is a valid memory size

Check that parameter is a valid memory size

## Usage

``` r
is.valid.memsize(x)
```

## Arguments

- x:

  A string representing a memory size (e.g. "8g")
