# Get corresponding R2 file. Assumes that the input file is R1

Get corresponding R2 file. Assumes that the input file is R1

## Usage

``` r
getFastqR2fromR1(fname)
```

## Arguments

- fname:

  Path to file

## Value

Path to R2 file
