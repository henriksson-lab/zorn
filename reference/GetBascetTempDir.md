# Get a temp directory to use; need to be created

Get a temp directory to use; need to be created

## Usage

``` r
GetBascetTempDir(bascetInstance)
```

## Arguments

- bascetInstance:

  A Bascet instance

## Value

A path to a temp directory that can be created. Must be removed when
done
