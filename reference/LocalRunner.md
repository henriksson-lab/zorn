# Create new local runner instance

Create new local runner instance

## Usage

``` r
LocalRunner(maxcpu = "10", direct = TRUE, showScript = FALSE)
```

## Arguments

- maxcpu:

  Number of CPUs to use (max)

- direct:

  Run jobs synchronously

- showScript:

  Show the script to run, for debugging purposes

## Value

A runner instance
