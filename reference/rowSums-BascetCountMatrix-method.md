# Row sums of a BascetCountMatrix (counts per cell)

Row sums of a BascetCountMatrix (counts per cell)

## Usage

``` r
# S4 method for class 'BascetCountMatrix'
rowSums(x)
```

## Arguments

- x:

  A BascetCountMatrix object

## Value

Named numeric vector of row sums
