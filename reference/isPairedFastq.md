# Check if a file is a paired FASTQ file. Panics if the file is not a FASTQ at all

Check if a file is a paired FASTQ file. Panics if the file is not a
FASTQ at all

## Usage

``` r
isPairedFastq(fname)
```

## Arguments

- fname:

  Path to file

## Value

TRUE if the file is a paired FASTQ
